package com.bloom.metaRepository.actions;

public class EnumProperties
  extends ActionableProperties
{
  protected EnumProperties()
  {
    setActionableFieldType(ActionableProperties.ActionableFieldType.ENUM);
  }
}
